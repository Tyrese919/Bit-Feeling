<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/bitLightBlue</item>
        <item name="colorPrimaryDark">@color/bitLightBlue</item>
        <item name="colorAccent">@color/bitWhite</item>

    </style>
</resources>
